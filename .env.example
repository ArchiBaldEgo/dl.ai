SECRET_KEY='change-me-please'

# Django (prod)
DEBUG='0'
ALLOWED_HOSTS='dl.gsu.by,dlai.gsu.by,127.0.0.1,localhost'
CSRF_TRUSTED_ORIGINS='https://dl.gsu.by,https://dlai.gsu.by'
USE_X_FORWARDED_PROTO='1'

DB_USER='dlaibd'
DB_PASSWORD='change-me-db-password'
DB_NAME='dl_ai'
DB_HOST='db'
DB_PORT='5432'

# Proxy (optional). If not needed - leave empty.
HTTP_PROXY=''
HTTPS_PROXY=''
ALL_PROXY=''
NO_PROXY='localhost,127.0.0.1,db,web,nginx'
PROXY=''

# Build-time proxy for Docker image build (optional).
# If you need a proxy for `apt-get`/`pip install` during `docker compose build`,
# set these. Otherwise leave empty.
BUILD_HTTP_PROXY=''
BUILD_HTTPS_PROXY=''
BUILD_ALL_PROXY=''
BUILD_NO_PROXY=''

CLIENT_ID=''
SBER_SECRET=''
HF_TOKEN=''
SC_TOKEN=''
# Optional alias for SC_TOKEN (same value):
SAMBANOVA_API_KEY=''
MIST_TOKEN=''
GROQ_TOKEN=''
