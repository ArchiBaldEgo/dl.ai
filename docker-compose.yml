version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dl_ai_web
    command: daphne -b 0.0.0.0 -p 8000 DjangoTest.asgi:application
    restart: unless-stopped
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
    networks:
      # Добавьте это
      - shared-network # Подключаем к общей сети

  db:
    image: postgres:14
    container_name: dl_ai_db
    restart: unless-stopped
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: dlaibd
      POSTGRES_PASSWORD: dlaibd
      POSTGRES_DB: dlaibd

  nginx:
    image: nginx:latest
    container_name: dl_ai_nginx
    restart: unless-stopped
    ports:
      - "80:8000"
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - static_volume:/app/static
    depends_on:
      - web
    networks:
      # Добавьте это
      - shared-network

volumes:
  postgres_data:
  static_volume:


networks:
  # Добавьте эту секцию
  shared-network:
    external: true # Используем ту же внешнюю сеть
